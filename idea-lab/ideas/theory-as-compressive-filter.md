---
id: idea-20260822-theory-as-compressive-filter
title: "Theory as a Compressive Filter"
tags:
- epistemology
- philosophy-of-science
- modeling
- abstraction
- self-reference
summary: "The note defines human thought-plus-time as constant manifold A and observed phenomena-plus-time as constant manifold B, with time folded into each manifold so change appears only as variation between slices, and since cognition is itself a phenomenon, A is a subset of B. A theory is a filter that uses one component of A to examine B, yielding a compressed time-carrying B′; physics uses a time-translation-invariant filter while sociology is reflexive because observation itself changes A and B. The only criterion for a good theory is that it characterizes B accurately and broadly. Publication stability, the fixed point T = Φ(T), is optional and adopted only to simplify. A stable core exists just when that self-referential equation has an approximate fixed point, the strict generative core."
body_hash: "38887278"
connections:
  - type: idea
    slug: "interaction-as-dictionary"
importance: 0.5  # auto
---
**Two constant manifolds.** Human thought-plus-time is a constant manifold **A**; all observed phenomena-plus-time is a constant manifold **B**. "Constant" is a convention, not an empirical claim: time is folded into the manifold, so A and B are fixed objects by construction — "change" is only their slices varying along the time axis, and all empirical content lives in the differences between those slices. Because cognition is itself a phenomenon, **A ⊂ B**.

**A theory is a filter.** A filter = using one component of A to examine B, yielding a compressed **B′** in a lower-dimensional space that still carries time. A theory is just a filter that compresses well enough — a knowledge structure produced by thought, hence a component of A. Physics' filter is time-translation-invariant (its B′ slices are identical, so one slice generates all); sociology's is reflexive (since A ⊂ B, observation itself changes A and B).

**The only criterion.** A theory is good if and only if it characterizes B's properties — accurately and broadly. That is the sole test. A second criterion — *publication stability*, the fixed point **T = Φ(T)** — is optional, assumed only to simplify.

**The stable core.** Whether a stable core exists is just whether the self-referential equation **T = Φ(T)** has an (approximate) fixed point — the strict form of a "generative core."
